MATCH p=(d)<-[:Evolved*]-(pd) WHERE id(d)=? WITH DISTINCT pd RETURN PROPERTIES(pd)

MATCH dp=(d)<-[r:Evolved*]-() 
WHERE id(d)=? 
UNWIND nodes(dp) AS nd 
OPTIONAL MATCH (nd)<-[e:Evolved]-() 
RETURN DISTINCT  id(nd), PROPERTIES(nd), PROPERTIES(e)